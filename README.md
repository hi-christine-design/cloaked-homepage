# cloaked-homepage

Interactive prototype of the Cloaked "Digital Risk" home screen. Drag across the chart to scrub through time — the risk level, exposed-site count, and trend update live. Toggle Week / Month / Year to swap the dataset.

Single-file static site (`index.html`). No build step.
